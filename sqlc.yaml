version: "2"
sql:
  - engine: "postgresql"
    schema: "sqlc/migration/"
    queries: "sqlc/query/"
    gen:
      go:
        package: "db"
        out: "backend/internal/db"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_interface: true
